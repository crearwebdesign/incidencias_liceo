{
  "name": "Entorno Flutter y Supabase",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "dart-code.flutter",
        "dart-code.dart-code"
      ]
    }
  },

  "postCreateCommand": "mkdir -p ~/desarrollo && git clone https://github.com/flutter/flutter.git -b stable ~/desarrollo/flutter && echo 'export PATH=\"$PATH:$HOME/desarrollo/flutter/bin\"' >> ~/.bashrc && npm install -g supabase"
}